(()=>{'use strict';const groups=()=>[...document.querySelectorAll('.flow-group,.reuse-journey-block')];const targetFor=group=>{if(group.dataset.lineTarget)return Math.max(1,parseInt(group.dataset.lineTarget,10)||1);if(group.classList.contains('reuse-journey-block')){const blocks=[...group.parentElement.querySelectorAll(':scope > .reuse-journey-block')],i=blocks.indexOf(group);return i%2===0?3:2}const siblings=[...group.parentElement.querySelectorAll(':scope > .flow-group')],i=siblings.indexOf(group);return i===0?3:2};const sync=group=>{const cards=[...group.querySelectorAll('.flow-node')],target=cards[targetFor(group)-1],first=cards[0];if(!first||!target)return;const g=group.getBoundingClientRect(),f=first.getBoundingClientRect(),t=target.getBoundingClientRect();const left=Math.max(0,f.left-g.left),width=Math.max(0,t.right-f.left);group.style.setProperty('--line-left',`${left}px`,'important');group.style.setProperty('--line-width',`${width}px`,'important');group.style.setProperty('--flow-line-left',`${left}px`,'important');group.style.setProperty('--flow-line-width',`${width}px`,'important')};const syncAll=()=>groups().forEach(sync);let raf=0;const queue=()=>{cancelAnimationFrame(raf);raf=requestAnimationFrame(syncAll)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',queue,{once:true});else queue();addEventListener('load',queue,{once:true});addEventListener('resize',queue,{passive:true});if('ResizeObserver'in window){const ro=new ResizeObserver(queue);groups().forEach(g=>ro.observe(g));document.querySelectorAll('.flow-row').forEach(r=>ro.observe(r))}if(document.fonts?.ready)document.fonts.ready.then(queue).catch(()=>{});})();